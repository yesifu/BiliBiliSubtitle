# 隐私说明

- API Key 与偏好仅保存于浏览器扩展的 `chrome.storage.local`，不进行 Chrome 账号同步。该存储允许的访问级别为受信任的扩展页面，网页内容脚本不能直接读取。Key 属于本机明文配置，请保护你的浏览器用户目录。
- 成功识别片段、完整原文、翻译批次、字幕译文、视频标题、BV / CID 与时间轴保存在本地 IndexedDB。设置页面可查看占用并清空字幕缓存。任务状态仅记录视频、进度和处理结果，不保存 Key。
- 开始生成任务时，插件通过现有浏览器登录态向 B 站请求视频信息、原生字幕及音轨地址。B 站登录 Cookie 不会发送到识别或翻译服务。
- 没有可复用字幕时，完整原始音轨或音频片段会发送到你配置的语音识别服务。开启翻译时，字幕文字及相邻上下文会发送到你配置的翻译服务。提供商如何保存与处理数据取决于其自身政策。
- 原始音频只在内存中处理，不写入字幕缓存。扩展没有分析统计、广告、开发者服务器或遥测上传。
- 全自动识别与下一 P 跟随默认关闭。用户开启后，插件在访问视频或播放时自动处理当前 / 下一 P；有可用原生字幕时不发送给识别或翻译服务。翻译始终受独立开关控制。关闭相应开关会停止对应的自动任务。
- 自动队列、已尝试任务标识、分 P 多选记录保存在浏览器会话存储 `chrome.storage.session`，不包含 Key，关闭浏览器后清除。
- 「刷新模型」仅将密钥发送给所配置的模型服务，读取可用模型列表。注册入口指向作者提供的硅基流动邀请链接 `https://cloud.siliconflow.cn/i/cW7ksWMh`；只有用户点击才打开，不会附加 Key 或视频内容。
- 默认仅授权 B 站、相关 CDN 和硅基流动域名。自定义接口需要在保存设置时授权所填域名。请求头规则只修改插件发往 B 站与 CDN 的请求，不修改普通网页请求。
- 清空缓存不删除 Key；如需移除 Key，可在设置中清空并保存。卸载扩展会移除扩展本地存储。
