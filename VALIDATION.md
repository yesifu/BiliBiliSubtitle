# 验证记录

## 1.3.0 自动识别与自由选择

2026-09-07：39 个功能测试通过，1 个可选 FFmpeg 路径测试跳过。22 个脚本语法、导入和 MV3 CSP 检查通过。

隔离 Microsoft Edge / Chromium 真实 MV3 扩展环境与模拟服务验证：

- 移除高级参数，三个档位、模型选择与刷新、邀请注册链接正确；窄屏无横向溢出。
- 真实内容脚本在视频进入时触发自动任务；原生字幕跳过、识别请求与翻译请求均为零。无原生字幕时正常识别；翻译关闭时不调用翻译接口。
- 播放当前 P 仅预识别下一 P，不递归处理全部目录。弹窗修改与设置页状态同步。
- 200 P 列表搜索、多选、清空、全选搜索结果、重新打开保留选择；仅执行已选的 P1 / P200，未运行其他章节。
- 实际 704 秒 M4A 测试素材：整段上传返回模拟 415 后换压缩分段，压缩分段再返回 415 后换 WAV，最终完成；原始音轨仍可正常解码。
- 原有字幕播放、识别缓存、翻译开关、翻译失败后原文可用、任务取消、合集、SRT 导出验证全部通过。

单元测试另行覆盖自动去重、手动任务优先、队列取消、自动任务关闭与重新开启、选中目录校验、格式回退的鉴权/限流/取消停止条件、识别/翻译模型分类查询。未使用真实用户 Key 或付费请求。硅基流动官方接口文档已核实，实际账户可用模型请使用刷新功能获取。

## 1.2.1 简化界面

删除设置页和弹窗中的宣传语、装饰字、侧栏和重复说明，改为紧凑表单。翻译配置随开关收起；字幕样式和高级设置默认折叠。关闭翻译时，未填完的翻译配置不阻止保存识别设置。

真实 MV3 浏览器加模拟接口验证通过：设置保存、翻译配置展开/收起及输入保留、折叠字段出错后自动展开并聚焦、390px 窄屏无横向溢出。原有识别、翻译、缓存、字幕显示、取消、合集、导出和翻译开关验证全部通过。检查了设置页和弹窗截图；18 个脚本、模块依赖与 CSP 检查通过。未使用真实 API Key 或付费请求。

## 1.2.0 翻译开关

默认关闭翻译，旧设置只迁移一次，明确开启后保持选择。独立原文缓存不依赖翻译语言、模型或 Key。增加两项设置/缓存回归测试。

真实 MV3 浏览器加模拟提供商验证：弹窗关闭开关后重新打开仍关闭；翻译失败时已有原文可直接显示/导出；关闭后生成不发出翻译请求也不重复 ASR；翻译正在等待时关闭开关可立即停止翻译并完成原文任务。保留其它字幕、队列、取消和导出验证。

## 1.1.0 整段直传版本

2026-09-07：31 项测试中 30 项通过，1 项独立 FFmpeg 测试因未设置路径跳过；FFmpeg 解码也已由辅助验证独立跑通。另行使用实际 Chromium `AudioContext.decodeAudioData` 验证用户视频音轨生成的全部 6 个独立 M4A，全部解码成功，时长与源样本边界一致。

- 真实视频：BV13Q4y1C7hS / CID 415333708，200 个分 P，最低码率 AAC 音轨 5,919,506 字节、703.9997 秒。
- 本机下载一次约 0.35 秒；无转码分段约 0.08–0.10 秒，6 段，全部 33,000 个 AAC 包在分段后保持字节相同、无重复遗漏。
- 新增测试覆盖整段一次上传且字节不变、连接卡住切换 CDN、明确区分翻译超时、翻译自适应缩小批量、按字幕复用已完成译文、成功识别检查点、兼容接口大小阈值、无关切片设置不使整段结果失效。
- 实际加载 MV3 扩展并以模拟模型接口验证：一次整段上传；翻译返回 401 后阶段提示正确；重试不重复下载识别；真实 AAC 音轨无转码分 6 段走完 offscreen → 上传 → 翻译 → 缓存流程。之前的播放器、合集、关闭弹窗继续工作、取消与导出验证仍通过。
- `npm run check` 已包含本地 vendor 的 `.mjs` 模块，18 个脚本语法、模块依赖与 CSP 检查通过。

没有实际调用付费 ASR / 翻译服务，无法验证硅基流动端点的 M4A 接受情况、实际云端耗时或模型识别质量。完整调查与官方来源见 `RESEARCH.md`。

## 1.0.0 初始版本

2026-09-07，Windows，Node.js 24，独立 Chromium（Microsoft Edge）测试配置。未读取或修改日常浏览器用户目录，未使用真实 API Key。

- `npm test`：12 项通过。覆盖 WBI 签名、视频身份、合集与分 P、域名与 Key 隔离、缓存配置、源语言匹配、精确与估算时间轴、翻译条数校验、SRT / VTT、WAV 编码、并发顺序与取消启动竞态。
- `npm run check`：Manifest 文件、所有脚本语法、模块依赖和 MV3 CSP 检查通过。
- `scripts/browser-smoke.mjs`：实际加载 MV3 扩展，验证设置保存、DNR 规则、IndexedDB、Offscreen 文档、完整音频解码与切片、所有识别完成后翻译、双语叠层、切 P 隐藏旧字幕、部分批量失败、缓存命中、取消任务、原生字幕复用、关闭弹窗后合集继续处理、重新打开恢复状态、实际 SRT 下载与清缓存。该项使用模拟 B 站和模型接口，截图在 `.browser-test`。
- 实际 B 站接口只读验证：公开视频详情返回成功（含 10 个分 P），独立音轨接口返回有效 CDN 地址，小范围下载响应 HTTP 206。
- 硅基流动当前官方文档核实：`FunAudioLLM/SenseVoiceSmall`，`/v1/audio/transcriptions`，响应 `text`，每次文件上传不超过 50 MB / 1 小时。

没有执行真实付费语音/翻译请求，也没有对真实 B 站页面的所有播放器版本和登录状态逐一验证。模型账户权限、真实识别质量、估算时间轴准确度，以及特定视频的风控和媒体可用性，需要安装并填写自己的 Key 后验证。默认模型不返回时间戳，估算时间轴不是逐字强制对齐。
